<Window x:Class="DevKit.Views.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="操作日志"
        Width="820" Height="540"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource BgBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="4,0,4,10">
            <TextBlock DockPanel.Dock="Left" Text="所有检测与安装操作均记录在此。"
                       Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center" FontSize="12.5" />
            <Button DockPanel.Dock="Right" Content="打开日志文件" Padding="14,7"
                    Style="{StaticResource SecondaryButton}" Click="OnOpenFile" />
        </DockPanel>
        <Border Grid.Row="1" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="12"
                Effect="{StaticResource CardShadowSm}">
            <ListBox x:Name="LogList" FontFamily="Consolas, Microsoft YaHei UI"
                     FontSize="12" BorderThickness="0" Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" />
        </Border>
    </Grid>
</Window>
